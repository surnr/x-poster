{
  "name": "x-poster",
  "version": "1.0.0",
  "description": "Automated tweet generation from news sources using Cloudflare Workers and AI Gateway",
  "private": true,
  "dependencies": {
    "cheerio": "^1.0.0",
    "openai": "^6.9.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^24.10.1",
    "typescript": "5.9.2",
    "wrangler": "^4.51.0"
  },
  "scripts": {
    "cf-typegen": "wrangler types",
    "check": "tsc && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "build": "wrangler build"
  }
}
